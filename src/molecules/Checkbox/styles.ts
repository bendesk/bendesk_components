import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
  rootV: { alignItems: 'start !important' },
  rootH: { alignItems: 'start !important', marginRight: '28px' },
  label: { marginTop: '12px' }
}))
